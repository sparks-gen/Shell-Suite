# Shell Suite
**Aim:** Reconcile all custom commands/actions into a unified `zsh` and `bash` suite utility

Ongoing development, see [Development-Plan.md](Development-Plan.md)

## Quick Start

To install Shell Suite, simply run the following command anywhere on your
machine:

```shell
curl -fsSL <setup.sh-url> | bash
```
